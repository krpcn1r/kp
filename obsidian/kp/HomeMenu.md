# HomeMenu

> Главное меню после входа в систему.  
> Файл: `user/HomeMenu.h/.cpp`

← [[Пользовательский интерфейс]]

Точка входа в [[Управление клиентами]], [[Администрирование]], просмотр тарифов.  
Возвращает `HomeResult::LOGOUT` или `HomeResult::EXIT_APP` в `main.cpp`.
