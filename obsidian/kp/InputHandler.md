# InputHandler

> Захват клавиатурного ввода, коды клавиш.  
> Файл: `core/InputHandler.h/.cpp`

← [[Ядро системы]]

Обрабатывает расширенные коды (`224` + scan-code) для стрелок и Del.  
Используется во всех интерактивных экранах через `getExtKey()`.
